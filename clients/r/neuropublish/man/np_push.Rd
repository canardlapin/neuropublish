% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{np_push}
\alias{np_push}
\title{Publish a packed bundle with \verb{npub push}}
\usage{
np_push(bundle, project, server, message = NULL, parent = NULL, token = NULL)
}
\arguments{
\item{bundle}{A packed bundle directory (from \code{\link[=np_pack]{np_pack()}}).}

\item{project}{\code{"workspace/project"}.}

\item{server}{The control-plane URL.}

\item{message}{Publication message.}

\item{parent}{Parent revision id; \code{NULL} for a project's first revision.}

\item{token}{Optional bearer (discouraged on the command line; prefer
\code{NP_TOKEN} or \code{\link[=np_login]{np_login()}}).}
}
\value{
A list with \code{revision} (id), \code{digest}, \code{revision_url}, \code{view_url},
\code{parent} (as the server recorded it), \code{unchanged}, and \code{output} (the
CLI's progress lines). Any other failure is an error of class
\code{np_cli_error} whose \code{type} is the server's error code or the CLI's
failure type.
}
\description{
Uploads the assets the server lacks and commits one immutable revision whose
parent is the project's current head. Authentication is the CLI's: an
explicit \code{token}, else \code{NP_TOKEN}, else the credential stored by
\code{\link[=np_login]{np_login()}}.
}
\details{
A push rejected because \code{parent} is no longer the head raises an error of
class \code{np_stale_parent} whose \code{head} field is the current head revision id;
re-run with \code{parent = head} (after deciding the new revision still makes
sense on top of it). Re-pushing bytes that already are the head is not an
error: the result has \code{unchanged = TRUE}.
}
\examples{
\dontrun{
r <- np_push("speech-model.npub", "rotman/sherlock",
  server = "http://127.0.0.1:8080", message = "Group model"
)
r$view_url
}
}
