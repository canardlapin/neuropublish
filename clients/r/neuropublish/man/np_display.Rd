% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{np_display}
\alias{np_display}
\alias{np_threshold}
\alias{np_window}
\title{Published display recommendation}
\usage{
np_display(threshold, window, colormap, opacity = NULL)

np_threshold(mode = c("two-sided", "positive", "negative", "off"), min = 0)

np_window(min, max, centre = NULL)
}
\arguments{
\item{threshold}{\code{list(mode, min)} with mode one of \code{"two-sided"},
\code{"positive"}, \code{"negative"}, \code{"off"} and \code{min >= 0}; see \code{\link[=np_threshold]{np_threshold()}}.}

\item{window}{\code{list(min, centre, max)} with \code{min < max}; see \code{\link[=np_window]{np_window()}}.}

\item{colormap}{Colormap name (a closed URL-safe grammar), e.g. \code{"cold-hot"}.}

\item{opacity}{Optional opacity in \verb{[0, 1]}.}

\item{mode}{Threshold mode.}

\item{min}{Threshold magnitude (non-negative).}

\item{max}{Window maximum.}

\item{centre}{Optional window centre.}
}
\value{
A plain list.
}
\description{
A display recommendation for a field. It is never an inferential decision:
the viewer treats it as the starting threshold and window.
}
\examples{
np_display(np_threshold("two-sided", 3.1), np_window(-8, 8), "cold-hot")
}
