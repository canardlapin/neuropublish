% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{np_field}
\alias{np_field}
\title{Result field record}
\usage{
np_field(
  id,
  estimand,
  measure,
  domain,
  selection = list(level = "group"),
  representations,
  order = NULL,
  published_display = NULL,
  label = NULL
)
}
\arguments{
\item{id}{Field id.}

\item{estimand}{The id of an estimand declared by some analysis.}

\item{measure}{A semantic id; use the constants in \link{np_measure}.}

\item{domain}{The id of the domain the representations live on.}

\item{selection}{A named list of axis id to axis value (e.g.
\code{list(level = "group")}); an empty selection is written as \code{{}}.}

\item{representations}{A list of representations, e.g.
\code{list(np_volume_rep("speech-t"))}.}

\item{order}{Optional display order within the estimand.}

\item{published_display}{Optional \code{\link[=np_display]{np_display()}} recommendation.}

\item{label}{Optional label.}
}
\value{
A plain list serializing to one entry of \code{resultFields}.
}
\description{
A result field is one measure of one estimand on one domain, with at least
one representation (for the volume MVP, a NIfTI asset).
}
\examples{
np_field("speech-t", "speech", np_measure$t_statistic, "mni-2mm",
  representations = list(np_volume_rep("speech-t")), order = 1
)
}
