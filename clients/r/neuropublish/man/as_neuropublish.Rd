% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{as_neuropublish}
\alias{as_neuropublish}
\alias{as_neuropublish.list}
\title{Describe an R object as a Neuropublish result}
\usage{
as_neuropublish(x, ...)

\method{as_neuropublish}{list}(
  x,
  title,
  synopsis,
  measures,
  sensitivity = "group-level",
  estimand = "effect",
  estimand_label = "effect",
  analysis_id = "analysis",
  analysis_label = "Analysis",
  method = NULL,
  sample_size = NULL,
  underlay = NULL,
  underlay_label = "Underlay",
  domain_id = "volume",
  space_name = "MNI152NLin2009cAsym",
  display = list(),
  staging = np_staging_dir(),
  ...
)
}
\arguments{
\item{x}{The object to describe.}

\item{...}{Passed to methods.}

\item{title, synopsis, sensitivity}{Passed to \code{\link[=np_manifest]{np_manifest()}}.}

\item{measures}{A named character vector mapping each volume's name to its
measure id (see \link{np_measure}); a volume without a measure is an underlay
when \code{underlay} names it and an error otherwise.}

\item{estimand, estimand_label}{The single estimand every field reports.}

\item{analysis_id, analysis_label}{The single analysis.}

\item{method}{An open record describing the method (\code{\link[=np_record]{np_record()}}), or
NULL.}

\item{sample_size}{Optional number of inputs.}

\item{underlay}{Optional name of the volume in \code{x} to declare as the
anatomical underlay (with \code{underlay_label}).}

\item{underlay_label}{Label of the underlay.}

\item{domain_id, space_name}{Passed to \code{\link[=np_domain_volume]{np_domain_volume()}}.}

\item{display}{Optional named list of \code{\link[=np_display]{np_display()}} recommendations keyed
by volume name.}

\item{staging}{Where NIfTI files are written; see \code{\link[=np_asset_volume]{np_asset_volume()}}.}
}
\value{
A \code{neuropublish_result}.
}
\description{
The generic the domain packages implement. A method receives whatever the
package considers a publishable result (an \code{fmrigds} group reduction, an
\code{rMVPA} searchlight, a \code{neuromosaic} report, ...) and returns a
\code{neuropublish_result} built with \code{\link[=np_result]{np_result()}}:
}
\details{
\itemize{
\item \code{manifest}: a manifest list from \code{\link[=np_manifest]{np_manifest()}} whose \code{assets} entries
name their bytes by \code{path} (see \code{\link[=np_asset_volume]{np_asset_volume()}}); everything else is
the core 0.1 vocabulary plus whatever extension fields the package adds;
\item \code{assets}: the same asset entries, as a list, so callers can see what will
be uploaded without walking the manifest.
}

The contract is: the manifest must be admitted by \verb{npub validate} after
\code{\link[=np_write_bundle]{np_write_bundle()}} and \code{\link[=np_pack]{np_pack()}}; every \verb{resultFields[].estimand} names
an estimand of an \code{analyses[]} entry; every field and underlay names a
declared domain and asset; the method and the provenance activities are
open records (\code{\link[=np_record]{np_record()}}) from the package's own namespace. Nothing in
the result may be a NaN or infinite number. \code{\link[=np_publish]{np_publish()}} does the rest.
}
\section{The \code{list} method}{


\code{as_neuropublish.list} is the reference implementation, and it is \emph{not} a
fallback for arbitrary lists: it describes one specific input, a \strong{named
list of \code{neuroim2::NeuroVol} objects that all live on the same grid} (same
dimensions and same \code{neuroim2::trans()}). Each name is used as an asset id,
as the id of the result field built from it, and as the key into \code{measures}
(and \code{display}); a volume named by \code{underlay} becomes the anatomical
underlay instead of a field, and every other volume needs an entry in
\code{measures}. From that it builds one volume domain from the first volume's
space, one asset per volume, one analysis with one estimand, and one result
field per volume with the measure you named for it.

A list holding anything else is an error naming the class that was found;
that is not a defect to work around but the signal that the object needs its
own method. Give the object a class and write \verb{as_neuropublish.<class>()}
for it: whatever it holds (a fitted model, a searchlight result, a report),
the method's job is to return the \code{np_result()} described above, and it may
call this method for the volume part of the work.
}

\examples{
sp <- neuroim2::NeuroSpace(c(8L, 8L, 6L), spacing = c(2, 2, 2), origin = c(-7, -7, -5))
vols <- list(
  "speech-t" = neuroim2::NeuroVol(array(rnorm(384), c(8, 8, 6)), sp)
)
r <- as_neuropublish(vols,
  title = "Example", synopsis = "One t-map.",
  measures = c("speech-t" = np_measure$t_statistic), staging = tempdir()
)
r$manifest$resultFields[[1]]$measure
}
