% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{np_record}
\alias{np_record}
\title{Open record: a schema reference with a payload}
\usage{
np_record(schema_id, version, payload = list(), digest = NULL)
}
\arguments{
\item{schema_id}{Semantic id of the schema, \code{namespace/name}.}

\item{version}{Version string \code{major.minor}.}

\item{payload}{A named list (may be empty).}

\item{digest}{Optional \verb{sha256:} digest of the schema document.}
}
\value{
A plain list \code{list(schema = list(id, version, digest), payload)}.
}
\description{
An open record is how the protocol carries structured content it does not
define itself: the \code{method} of an analysis and provenance activities are
open records. The \code{schema} names the document that defines the payload; a
digest is checked only for schemas in the trusted namespace, so a record
from your own namespace is retained as-is (the viewer shows it, but grants
it no behaviour).
}
\examples{
np_record("org.example.lab/smooth", "0.3", list(fwhmMm = 6))
}
