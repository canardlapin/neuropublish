% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assets.R
\name{np_asset_volume}
\alias{np_asset_volume}
\title{Volume asset from a \code{NeuroVol} or a NIfTI file}
\usage{
np_asset_volume(id, x, staging = np_staging_dir(), catalog = NULL, ...)
}
\arguments{
\item{id}{Asset id, referenced by fields and underlays.}

\item{x}{A \code{neuroim2::NeuroVol} or the path of an existing NIfTI file
(\code{.nii} or \code{.nii.gz}).}

\item{staging}{Directory that receives the NIfTI written from a \code{NeuroVol}
(default: a session-scoped temporary directory).}

\item{catalog}{Optional catalog reference (informational once a digest is
present, e.g. \code{"templateflow:MNI152NLin2009cAsym/T1w/res-02"}).}

\item{...}{Further members added to the record as given (extension fields).}
}
\value{
A plain list with \code{id}, \code{mediaType}, \code{path} (and \code{catalog}, \code{...}).
}
\description{
Declares one \code{assets[]} entry for a staging bundle. A \code{neuroim2::NeuroVol}
is written to NIfTI (float32) in \code{staging} right away; an existing file is
referenced as it is. The entry carries \code{path} (the file) instead of
\code{digest}/\code{size}, which \verb{npub pack} fills in after hashing the bytes
(\code{\link[=np_pack]{np_pack()}}); \code{\link[=np_write_bundle]{np_write_bundle()}} copies the file under \verb{assets/} of the
staging bundle and rewrites \code{path} relative to it.
}
\examples{
sp <- neuroim2::NeuroSpace(c(4L, 4L, 4L), spacing = c(2, 2, 2), origin = c(-3, -3, -3))
vol <- neuroim2::NeuroVol(array(seq_len(64), c(4, 4, 4)), sp)
a <- np_asset_volume("tiny", vol, staging = tempdir())
a$mediaType
file.exists(a$path)
}
