% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domain.R
\name{np_volume_grid_fingerprint}
\alias{np_volume_grid_fingerprint}
\title{Structural fingerprint of a volume grid (ADR 0005)}
\usage{
np_volume_grid_fingerprint(
  shape,
  affine,
  space,
  convention = "RAS+",
  unit = "mm",
  layout = "x-fastest",
  descriptor = np_volume_grid_schema
)
}
\arguments{
\item{shape}{Integer vector of length 3: the grid dimensions.}

\item{affine}{A 4 x 4 numeric matrix mapping voxel indices to world
coordinates (as \code{neuroim2::trans()} returns for a \code{NeuroSpace}).}

\item{space}{Coordinate space name (e.g. \code{"MNI152NLin2009cAsym"}).}

\item{convention}{Coordinate convention; the protocol defines \code{"RAS+"}.}

\item{unit}{Spatial unit; \code{"mm"}.}

\item{layout}{Ordinal layout of voxels in the volume; \code{"x-fastest"}.}

\item{descriptor}{The descriptor schema reference; defaults to
\link{np_volume_grid_schema}.}
}
\value{
A character scalar \code{"sha256:<64 hex digits>"}.
}
\description{
Computes the \code{structuralFingerprint} of a \code{volume-grid/v1} domain key from
the documented preimage: the eight bytes \verb{NPUDOM1\\0}, six length-prefixed
UTF-8 strings (descriptor id, descriptor version, space, coordinate
convention, spatial unit, ordinal layout), the three shape entries as
little-endian Int32, and the sixteen affine entries in row-major order as
little-endian Float64 with negative zero normalised to zero; the fingerprint
is \verb{sha256:} followed by the hex SHA-256 of those bytes. This is the same
computation the Julia producer (\code{modules/conformance/julia/producer.jl}) and
the Scala server perform, so the three agree byte for byte.
}
\examples{
affine <- diag(c(2, 2, 2, 1))
affine[1:3, 4] <- c(-22, -30, -18)
np_volume_grid_fingerprint(c(24L, 28L, 20L), affine, "MNI152NLin2009cAsym")
}
