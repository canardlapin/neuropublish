% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{np_npub_bin}
\alias{np_npub_bin}
\title{Locate the \code{npub} command-line tool}
\usage{
np_npub_bin(required = TRUE)
}
\arguments{
\item{required}{Stop with an informative error when nothing is found
(default); otherwise return \code{NULL}.}
}
\value{
The path of the executable, or \code{NULL}.
}
\description{
Resolution order: the \code{NPUB_BIN} environment variable, then \code{npub} on the
\code{PATH}, then \code{scripts/npub} of a Neuropublish checkout found by walking up
from the working directory (the launcher that exports the sbt classpath
once and runs \code{neuropublish.npub.Main}).
}
\examples{
np_npub_bin(required = FALSE)
}
