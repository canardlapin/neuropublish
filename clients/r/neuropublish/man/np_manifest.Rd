% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{np_manifest}
\alias{np_manifest}
\title{Start a manifest}
\usage{
np_manifest(
  title,
  synopsis,
  sensitivity = "group-level",
  axes = list(np_axis("level", "Level", "group")),
  notes = NULL,
  ...
)
}
\arguments{
\item{title}{The publication title.}

\item{synopsis}{One or two sentences for the revision list.}

\item{sensitivity}{\code{"group-level"} or \code{"subject-level"}. Only group-level
revisions can be link-shared.}

\item{axes}{A list of axis records, each \code{list(id, label, values)}; the
default declares a single \code{level} axis with the one value \code{"group"}, which
is what the selection of every field refers to.}

\item{notes}{Optional free-text notes.}

\item{...}{Further members added to the manifest as given.}
}
\value{
A plain list.
}
\description{
Creates the top-level manifest list with \code{core = "0.1"}, the title, synopsis
and sensitivity, and empty collections to fill with \code{\link[=np_domain_volume]{np_domain_volume()}},
\code{\link[=np_domain_finite]{np_domain_finite()}}, \code{\link[=np_domain_mapping_hard]{np_domain_mapping_hard()}}, \code{\link[=np_asset_volume]{np_asset_volume()}},
\code{\link[=np_analysis]{np_analysis()}}, \code{\link[=np_field]{np_field()}}, \code{\link[=np_underlay]{np_underlay()}},
\code{\link[=np_warning]{np_warning()}} and \code{\link[=np_provenance]{np_provenance()}}. Anything passed through \code{...} is added
verbatim (an extension field such as \code{`x-lab-run`}), and you may also
add members to the returned list directly: unknown fields survive
\code{\link[=np_write_bundle]{np_write_bundle()}}, \verb{npub pack}, and the server.
}
\examples{
m <- np_manifest("Speech model", "Group t-map of the speech regressor.")
names(m)
}
