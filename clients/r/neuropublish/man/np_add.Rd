% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{np_add}
\alias{np_add}
\title{Append a record to a manifest collection}
\usage{
np_add(manifest, collection, record)
}
\arguments{
\item{manifest}{A manifest from \code{\link[=np_manifest]{np_manifest()}}.}

\item{collection}{One of \code{domains}, \code{assets}, \code{analyses}, \code{resultFields},
\code{underlays}, \code{warnings}.}

\item{record}{A record built with the matching \verb{np_*()} builder.}
}
\value{
The updated manifest.
}
\description{
Convenience for building a manifest incrementally: appends \code{record} to
\code{manifest[[collection]]} and returns the manifest. Records are checked for
duplicate ids within their collection.
}
\examples{
m <- np_manifest("Speech model", "Group t-map.")
m <- np_add(m, "warnings", np_warning("w1", "An example warning."))
length(m$warnings)
}
