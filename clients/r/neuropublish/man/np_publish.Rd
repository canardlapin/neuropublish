% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{np_publish}
\alias{np_publish}
\title{Publish an R object as one revision}
\usage{
np_publish(
  x,
  project,
  server,
  message = NULL,
  parent = NULL,
  dir = tempfile("neuropublish-"),
  ...
)
}
\arguments{
\item{x}{An object with an \code{\link[=as_neuropublish]{as_neuropublish()}} method, or a
\code{neuropublish_result}.}

\item{project}{\code{"workspace/project"}.}

\item{server}{The control-plane URL.}

\item{message}{Publication message.}

\item{parent}{Parent revision id, or NULL for the first revision.}

\item{dir}{Working directory for the staging and packed bundles (default: a
fresh temporary directory). The packed bundle is \verb{<dir>/bundle.npub}.}

\item{...}{Passed to \code{\link[=as_neuropublish]{as_neuropublish()}}.}
}
\value{
The \code{\link[=np_push]{np_push()}} result, with \code{bundle} (the packed directory) added.
}
\description{
\code{as_neuropublish()} the object, write the staging bundle, \verb{npub pack} it,
and \verb{npub push} the packed bundle: the one high-level call after
\code{\link[=np_login]{np_login()}}.
}
\examples{
\dontrun{
np_publish(vols, "rotman/sherlock", server = "http://127.0.0.1:8080",
  title = "Example", synopsis = "...", measures = c("speech-t" = np_measure$t_statistic)
)
}
}
