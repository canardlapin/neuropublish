% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{np_validate}
\alias{np_validate}
\title{Validate a bundle with \verb{npub validate}}
\usage{
np_validate(dir)
}
\arguments{
\item{dir}{A bundle directory.}
}
\value{
A data frame with one row per problem, columns \code{pointer} (JSON
Pointer into the manifest, \code{""} for the whole document) and \code{message}.
Zero rows means the bundle is admitted; the data frame then carries
attributes \code{digest} (the manifest digest) and \code{assets} (\code{"N declared, M volume"}). A failure that is not an admission problem (the bundle has no
\code{manifest.json}, \code{npub} cannot run) is an error of class \code{np_cli_error},
never a problem row.
}
\description{
Runs the server's admission on the bundle's \code{manifest.json} bytes (schema,
reference closure, semantic invariants, trusted-record digests) without a
server. A staging bundle whose assets still carry \code{path} is \emph{not} valid
(\code{digest} and \code{size} are required): pack it first with \code{\link[=np_pack]{np_pack()}}, or
validate the packed output.
}
\examples{
\dontrun{
problems <- np_validate("speech-model.npub")
if (nrow(problems)) print(problems)
}
}
