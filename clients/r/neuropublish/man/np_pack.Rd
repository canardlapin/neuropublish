% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{np_pack}
\alias{np_pack}
\title{Pack a staging bundle with \verb{npub pack}}
\usage{
np_pack(staging, out, force = FALSE)
}
\arguments{
\item{staging}{The staging bundle written by \code{\link[=np_write_bundle]{np_write_bundle()}}.}

\item{out}{The packed bundle directory to create (conventionally \code{.npub}).}

\item{force}{Replace an existing \code{out}.}
}
\value{
A list with \code{dir} (the packed bundle), \code{digest} (the packed
manifest's digest) and \code{assets} (a data frame \code{id}, \code{size}, \code{digest}).
}
\description{
Hashes every asset file a staging manifest names by \code{path}, writes the
normalized bundle (\verb{assets/sha256/xx/<hex>}) and a manifest with \code{digest}
and \code{size} filled in and \code{path} removed, and admits the written bytes.
Nothing else in the manifest is touched, so unknown fields survive.
}
\examples{
\dontrun{
packed <- np_pack("staging", "speech-model.npub")
packed$digest
}
}
