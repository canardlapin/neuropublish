% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic.R
\name{np_synthetic_volumes}
\alias{np_synthetic_volumes}
\title{Synthetic volumes mirroring the Julia conformance producer}
\usage{
np_synthetic_volumes()
}
\value{
A named list of \code{neuroim2::NeuroVol} objects.
}
\description{
Builds the four volumes \code{modules/conformance/julia/producer.jl} writes
(\code{t1}, \code{speech-effect}, \code{speech-t}, \code{speech-z}) with the same formulas on
the same 16 x 16 x 12 grid (2 mm, origin -15, -15, -11), so values at any
voxel are known and the volume-grid fingerprint of their space equals the
one in \code{fixtures/julia/manifest.json}. Used by the vignette and the tests;
useful as a small, fully known example bundle.
}
\examples{
vols <- np_synthetic_volumes()
names(vols)
round(vols$`speech-t`[8, 8, 6], 4)
}
