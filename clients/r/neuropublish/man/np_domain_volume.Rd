% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domain.R
\name{np_domain_volume}
\alias{np_domain_volume}
\title{Describe a volume domain from a \code{NeuroSpace}}
\usage{
np_domain_volume(
  space,
  id = "volume",
  space_name = "MNI152NLin2009cAsym",
  convention = "RAS+",
  unit = "mm",
  layout = "x-fastest"
)
}
\arguments{
\item{space}{A \code{neuroim2::NeuroSpace} (or a \code{NeuroVol}, whose space is
used), three-dimensional.}

\item{id}{The domain id referenced by result fields and underlays.}

\item{space_name}{The coordinate space name recorded in the payload.}

\item{convention, unit, layout}{Payload values; the defaults are the only ones
the protocol currently defines for a \code{volume-grid} domain.}
}
\value{
A plain list that serializes to one entry of \code{domains}.
}
\description{
Builds the \code{domains[]} record for a regular 3-D grid: the open descriptor
(\verb{\{schema, payload\}} with the grid's shape, affine, space, convention, unit
and layout) and the exact \code{key} (\code{descriptor}, \code{size}, and the ADR 0005
\code{structuralFingerprint} computed by \code{\link[=np_volume_grid_fingerprint]{np_volume_grid_fingerprint()}}).
}
\examples{
sp <- neuroim2::NeuroSpace(c(24L, 28L, 20L), spacing = c(2, 2, 2), origin = c(-22, -30, -18))
d <- np_domain_volume(sp, id = "mni-2mm")
d$key$structuralFingerprint
}
